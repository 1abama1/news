# 📰 Новостной агрегатор

Современное веб-приложение для просмотра и поиска новостей с использованием TheNewsAPI. Приложение имеет темный дизайн, поддерживает поиск, фильтрацию по тегам, сортировку и бесконечную прокрутку.

## ✨ Возможности

- 🔍 **Поиск новостей** — поиск по заголовкам и описаниям статей
- 🏷️ **Фильтрация по тегам** — быстрая фильтрация по категориям
- 📊 **Сортировка** — сортировка по дате (новые/старые) и алфавиту
- ♾️ **Бесконечная прокрутка** — автоматическая загрузка новых статей при прокрутке
- 💾 **Кэширование** — сохранение статей в localStorage для быстрого доступа
- 📱 **Адаптивный дизайн** — корректное отображение на всех устройствах
- 🎨 **Современный UI** — темная тема с плавными анимациями и эффектами

## 🚀 Быстрый старт

### Требования

- Современный веб-браузер с поддержкой ES6+
- API ключ от [TheNewsAPI](https://www.thenewsapi.com/)

### Установка

1. Клонируйте репозиторий или скачайте файлы проекта
2. Откройте файл `src/js/main.js`
3. Замените значение `API_KEY` на ваш собственный ключ от TheNewsAPI:

```javascript
const API_KEY = "ваш_ключ_здесь";
```

4. Откройте `index.html` в браузере или запустите локальный сервер:

```bash
# Используя Python 3
python -m http.server 8000

# Используя Node.js (http-server)
npx http-server

# Используя PHP
php -S localhost:8000
```

5. Откройте в браузере: `http://localhost:8000`

## 📁 Структура проекта

```
news/
├── index.html              # Главная HTML страница
├── src/
│   ├── js/
│   │   └── main.js        # Основная логика приложения
│   ├── styles/
│   │   └── style.css      # Стили приложения
│   └── 3d0a1942ea617825e187c3c9a3811a5d93a331be-370x366.webp  # Иконка
└── README.md              # Документация
```

## ⚙️ Конфигурация

В файле `src/js/main.js` можно настроить следующие параметры:

- `API_KEY` — ключ API от TheNewsAPI
- `PER_PAGE` — количество статей на странице (по умолчанию: 12)
- `FALLBACK_QUERY` — запрос по умолчанию, если поиск пустой (по умолчанию: 'новости')
- `OBSERVER_MARGIN` — отступ для Intersection Observer (по умолчанию: '400px')
- `language` — язык новостей (по умолчанию: 'ru')

## 🎯 Использование

### Поиск

Введите запрос в поле поиска в верхней части страницы. Поиск выполняется автоматически с задержкой 350 мс после окончания ввода.

### Фильтрация по тегам

Нажмите на любой тег над лентой новостей, чтобы отфильтровать статьи по этой категории. Кнопка "все" сбрасывает фильтр.

### Сортировка

Используйте выпадающий список в заголовке для выбора способа сортировки:
- **Сначала новые** — по дате публикации (от новых к старым)
- **Сначала старые** — по дате публикации (от старых к новым)
- **По алфавиту A→Z** — по алфавиту заголовков

### Просмотр статей

Нажмите кнопку "Читать" на карточке статьи, чтобы открыть оригинальную статью в новой вкладке.

## 🛠️ Технологии

- **HTML5** — структура страницы
- **CSS3** — стилизация с использованием CSS переменных и анимаций
- **Vanilla JavaScript (ES6+)** — логика приложения
- **TheNewsAPI** — источник новостей
- **Intersection Observer API** — реализация бесконечной прокрутки
- **LocalStorage API** — кэширование данных

## 📝 Особенности реализации

- **Шаблонизация** — использование HTML `<template>` для создания карточек статей
- **Debounce** — задержка поиска для оптимизации запросов к API
- **Кэширование** — автоматическое сохранение загруженных статей в localStorage
- **Обработка ошибок** — отображение сообщений об ошибках при проблемах с API
- **Адаптивная сетка** — автоматическое изменение количества колонок в зависимости от размера экрана

## 🎨 Дизайн

Приложение использует темную цветовую схему с акцентами синего цвета:
- Фон: `#0F172A`
- Карточки: `#1E293B`
- Акцент: `#60A5FA` / `#1D4ED8`
- Шрифт: Archivo (Google Fonts)

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Вклад

Если вы хотите улучшить проект, создайте issue или pull request.

## 📞 Поддержка

При возникновении проблем проверьте:
1. Правильность API ключа
2. Наличие интернет-соединения
3. Консоль браузера на наличие ошибок

---

**Приятного использования!** 🚀

